import { createContext } from "react";

/** Instancia de contexto (archivo aparte para evitar conflicto de nombre con PermissionPreviewContext.jsx en Windows). */
export const PermissionPreviewContext = createContext(null);
